// Code generated by codegen/src/Packet.Sdl.CodeGen from spec-sdl/v2.2-errata/data-link/yaml/timer_recovery.sdl.yaml.
// DO NOT EDIT. Run `dotnet run --project codegen/src/Packet.Sdl.CodeGen` to regenerate.

package ax25sdl

import "testing"

func TestDataLinkTimerRecovery_SourceFigure(t *testing.T) {
	if got := DataLinkTimerRecovery.Source.Figure; got != "figc4.5" {
		t.Errorf("Source.Figure = %q, want figc4.5", got)
	}
}

func TestDataLinkTimerRecovery_TransitionsArePresent(t *testing.T) {
	if got := len(DataLinkTimerRecovery.Transitions); got != 92 {
		t.Errorf("len(Transitions) = %d, want 92", got)
	}
}

func TestDataLinkTimerRecovery_t01_dl_disconnect_request(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t01_dl_disconnect_request" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t01_dl_disconnect_request not found")
	}
	if tx.On != "DL_DISCONNECT_request" {
		t.Errorf("On = %q, want DL_DISCONNECT_request", tx.On)
	}
	if tx.Next != "AwaitingRelease" {
		t.Errorf("Next = %q, want AwaitingRelease", tx.Next)
	}
	if got := len(tx.Actions); got != 5 {
		t.Fatalf("len(Actions) = %d, want 5", got)
	}
	if tx.Actions[0].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "RC := 0" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "RC := 0")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "DISC (P = 1)" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "DISC (P = 1)")
	}
	if tx.Actions[2].Kind != SignalLower {
		t.Errorf("Actions[2].Kind = %v, want SignalLower", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Stop T3" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Stop T3")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Start T1" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Start T1")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
}

func TestDataLinkTimerRecovery_t02_dl_data_request(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t02_dl_data_request" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t02_dl_data_request not found")
	}
	if tx.On != "DL_DATA_request" {
		t.Errorf("On = %q, want DL_DATA_request", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if got := len(tx.Actions); got != 1 {
		t.Fatalf("len(Actions) = %d, want 1", got)
	}
	if tx.Actions[0].Verb != "Push on I Frame Queue" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Push on I Frame Queue")
	}
	if tx.Actions[0].Kind != InternalOut {
		t.Errorf("Actions[0].Kind = %v, want InternalOut", tx.Actions[0].Kind)
	}
}

func TestDataLinkTimerRecovery_t03_i_frame_pops_off_queue_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t03_i_frame_pops_off_queue_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t03_i_frame_pops_off_queue_yes not found")
	}
	if tx.On != "I_frame_pops_off_queue" {
		t.Errorf("On = %q, want I_frame_pops_off_queue", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "peer_receiver_busy" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "peer_receiver_busy")
	}
	if got := len(tx.Actions); got != 1 {
		t.Fatalf("len(Actions) = %d, want 1", got)
	}
	if tx.Actions[0].Verb != "Push I Frame on I Queue" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Push I Frame on I Queue")
	}
	if tx.Actions[0].Kind != InternalOut {
		t.Errorf("Actions[0].Kind = %v, want InternalOut", tx.Actions[0].Kind)
	}
}

func TestDataLinkTimerRecovery_t03_i_frame_pops_off_queue_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t03_i_frame_pops_off_queue_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t03_i_frame_pops_off_queue_no_yes not found")
	}
	if tx.On != "I_frame_pops_off_queue" {
		t.Errorf("On = %q, want I_frame_pops_off_queue", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not peer_receiver_busy and vs_eq_va_plus_k" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not peer_receiver_busy and vs_eq_va_plus_k")
	}
	if got := len(tx.Actions); got != 1 {
		t.Fatalf("len(Actions) = %d, want 1", got)
	}
	if tx.Actions[0].Verb != "Push I Frame on I Queue" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Push I Frame on I Queue")
	}
	if tx.Actions[0].Kind != InternalOut {
		t.Errorf("Actions[0].Kind = %v, want InternalOut", tx.Actions[0].Kind)
	}
}

func TestDataLinkTimerRecovery_t03_i_frame_pops_off_queue_no_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t03_i_frame_pops_off_queue_no_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t03_i_frame_pops_off_queue_no_no_yes not found")
	}
	if tx.On != "I_frame_pops_off_queue" {
		t.Errorf("On = %q, want I_frame_pops_off_queue", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not peer_receiver_busy and not vs_eq_va_plus_k and T1_running" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not peer_receiver_busy and not vs_eq_va_plus_k and T1_running")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "N(s) := V(s)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "N(s) := V(s)")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "N(r) := V(r)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "N(r) := V(r)")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "P := 0" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "P := 0")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "I Command" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "I Command")
	}
	if tx.Actions[3].Kind != SignalLower {
		t.Errorf("Actions[3].Kind = %v, want SignalLower", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "V(s) := V(s) + 1" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "V(s) := V(s) + 1")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t03_i_frame_pops_off_queue_no_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t03_i_frame_pops_off_queue_no_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t03_i_frame_pops_off_queue_no_no_no not found")
	}
	if tx.On != "I_frame_pops_off_queue" {
		t.Errorf("On = %q, want I_frame_pops_off_queue", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not peer_receiver_busy and not vs_eq_va_plus_k and not T1_running" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not peer_receiver_busy and not vs_eq_va_plus_k and not T1_running")
	}
	if got := len(tx.Actions); got != 8 {
		t.Fatalf("len(Actions) = %d, want 8", got)
	}
	if tx.Actions[0].Verb != "N(s) := V(s)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "N(s) := V(s)")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "N(r) := V(r)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "N(r) := V(r)")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "P := 0" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "P := 0")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "I Command" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "I Command")
	}
	if tx.Actions[3].Kind != SignalLower {
		t.Errorf("Actions[3].Kind = %v, want SignalLower", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "V(s) := V(s) + 1" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "V(s) := V(s) + 1")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "Stop T3" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "Stop T3")
	}
	if tx.Actions[6].Kind != Processing {
		t.Errorf("Actions[6].Kind = %v, want Processing", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "Start T1" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "Start T1")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
}

func TestDataLinkTimerRecovery_t04_dl_unit_data_request(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t04_dl_unit_data_request" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t04_dl_unit_data_request not found")
	}
	if tx.On != "DL_UNIT_DATA_request" {
		t.Errorf("On = %q, want DL_UNIT_DATA_request", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if got := len(tx.Actions); got != 1 {
		t.Fatalf("len(Actions) = %d, want 1", got)
	}
	if tx.Actions[0].Verb != "UI Command" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "UI Command")
	}
	if tx.Actions[0].Kind != SignalLower {
		t.Errorf("Actions[0].Kind = %v, want SignalLower", tx.Actions[0].Kind)
	}
}

func TestDataLinkTimerRecovery_t05_dl_flow_off_request_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t05_dl_flow_off_request_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t05_dl_flow_off_request_yes not found")
	}
	if tx.On != "DL_FLOW_OFF_request" {
		t.Errorf("On = %q, want DL_FLOW_OFF_request", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "own_receiver_busy" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "own_receiver_busy")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "Set Own Receiver Busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Set Own Receiver Busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "RNR Response (F = 0)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "RNR Response (F = 0)")
	}
	if tx.Actions[1].Kind != SignalLower {
		t.Errorf("Actions[1].Kind = %v, want SignalLower", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t05_dl_flow_off_request_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t05_dl_flow_off_request_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t05_dl_flow_off_request_no not found")
	}
	if tx.On != "DL_FLOW_OFF_request" {
		t.Errorf("On = %q, want DL_FLOW_OFF_request", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not own_receiver_busy" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not own_receiver_busy")
	}
	if got := len(tx.Actions); got != 0 {
		t.Fatalf("len(Actions) = %d, want 0", got)
	}
}

func TestDataLinkTimerRecovery_t06_dl_flow_on_request_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t06_dl_flow_on_request_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t06_dl_flow_on_request_yes_no not found")
	}
	if tx.On != "DL_FLOW_ON_request" {
		t.Errorf("On = %q, want DL_FLOW_ON_request", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "own_receiver_busy and not T1_running" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "own_receiver_busy and not T1_running")
	}
	if got := len(tx.Actions); got != 5 {
		t.Fatalf("len(Actions) = %d, want 5", got)
	}
	if tx.Actions[0].Verb != "Clear Own Receiver Busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Clear Own Receiver Busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "RR Command (P = 0)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "RR Command (P = 0)")
	}
	if tx.Actions[1].Kind != SignalLower {
		t.Errorf("Actions[1].Kind = %v, want SignalLower", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Stop T3" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Stop T3")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Start T1" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Start T1")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
}

func TestDataLinkTimerRecovery_t06_dl_flow_on_request_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t06_dl_flow_on_request_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t06_dl_flow_on_request_yes_yes not found")
	}
	if tx.On != "DL_FLOW_ON_request" {
		t.Errorf("On = %q, want DL_FLOW_ON_request", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "own_receiver_busy and T1_running" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "own_receiver_busy and T1_running")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "Clear Own Receiver Busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Clear Own Receiver Busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "RR Command (P = 0)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "RR Command (P = 0)")
	}
	if tx.Actions[1].Kind != SignalLower {
		t.Errorf("Actions[1].Kind = %v, want SignalLower", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t06_dl_flow_on_request_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t06_dl_flow_on_request_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t06_dl_flow_on_request_no not found")
	}
	if tx.On != "DL_FLOW_ON_request" {
		t.Errorf("On = %q, want DL_FLOW_ON_request", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not own_receiver_busy" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not own_receiver_busy")
	}
	if got := len(tx.Actions); got != 0 {
		t.Fatalf("len(Actions) = %d, want 0", got)
	}
}

func TestDataLinkTimerRecovery_t07_dl_connect_request(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t07_dl_connect_request" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t07_dl_connect_request not found")
	}
	if tx.On != "DL_CONNECT_request" {
		t.Errorf("On = %q, want DL_CONNECT_request", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Establish_Data_Link" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Establish_Data_Link")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Set Layer 3 Initiated" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Set Layer 3 Initiated")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t08_control_field_error(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t08_control_field_error" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t08_control_field_error not found")
	}
	if tx.On != "control_field_error" {
		t.Errorf("On = %q, want control_field_error", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (L)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (L)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Establish_Data_Link" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Establish_Data_Link")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Set Layer 3 Initiated" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Set Layer 3 Initiated")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t09_info_not_permitted_in_frame(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t09_info_not_permitted_in_frame" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t09_info_not_permitted_in_frame not found")
	}
	if tx.On != "info_not_permitted_in_frame" {
		t.Errorf("On = %q, want info_not_permitted_in_frame", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (M)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (M)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Establish_Data_Link" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Establish_Data_Link")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Set Layer 3 Initiated" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Set Layer 3 Initiated")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t10_u_or_s_frame_length_error(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t10_u_or_s_frame_length_error" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t10_u_or_s_frame_length_error not found")
	}
	if tx.On != "u_or_s_frame_length_error" {
		t.Errorf("On = %q, want u_or_s_frame_length_error", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (N)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (N)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Establish_Data_Link" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Establish_Data_Link")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Set Layer 3 Initiated" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Set Layer 3 Initiated")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t11_ua_received(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t11_ua_received" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t11_ua_received not found")
	}
	if tx.On != "UA_received" {
		t.Errorf("On = %q, want UA_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (N)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (N)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Establish_Data_Link" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Establish_Data_Link")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Clear Layer 3 Initiated" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Clear Layer 3 Initiated")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t12_dm_received(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t12_dm_received" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t12_dm_received not found")
	}
	if tx.On != "DM_received" {
		t.Errorf("On = %q, want DM_received", tx.On)
	}
	if tx.Next != "Disconnected" {
		t.Errorf("Next = %q, want Disconnected", tx.Next)
	}
	if got := len(tx.Actions); got != 5 {
		t.Fatalf("len(Actions) = %d, want 5", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (E)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (E)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "DL_DISCONNECT_indication" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "DL_DISCONNECT_indication")
	}
	if tx.Actions[1].Kind != SignalUpper {
		t.Errorf("Actions[1].Kind = %v, want SignalUpper", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Stop T1" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Stop T1")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Stop T3" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Stop T3")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
}

func TestDataLinkTimerRecovery_t13_sabm_received_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t13_sabm_received_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t13_sabm_received_no not found")
	}
	if tx.On != "SABM_received" {
		t.Errorf("On = %q, want SABM_received", tx.On)
	}
	if tx.Next != "Connected" {
		t.Errorf("Next = %q, want Connected", tx.Next)
	}
	if tx.Guard != "not vs_eq_va" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not vs_eq_va")
	}
	if got := len(tx.Actions); got != 12 {
		t.Fatalf("len(Actions) = %d, want 12", got)
	}
	if tx.Actions[0].Verb != "F := P" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "F := P")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "set_version_2_0" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "set_version_2_0")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "UA" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "UA")
	}
	if tx.Actions[2].Kind != SignalLower {
		t.Errorf("Actions[2].Kind = %v, want SignalLower", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Clear Exception Conditions" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Clear Exception Conditions")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "DL-ERROR Indication (F)" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "DL-ERROR Indication (F)")
	}
	if tx.Actions[4].Kind != SignalUpper {
		t.Errorf("Actions[4].Kind = %v, want SignalUpper", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "DL_CONNECT_indication" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "DL_CONNECT_indication")
	}
	if tx.Actions[6].Kind != SignalUpper {
		t.Errorf("Actions[6].Kind = %v, want SignalUpper", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "Stop T1" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "Stop T1")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
	if tx.Actions[8].Verb != "Stop T3" {
		t.Errorf("Actions[8].Verb = %q, want %q", tx.Actions[8].Verb, "Stop T3")
	}
	if tx.Actions[8].Kind != Processing {
		t.Errorf("Actions[8].Kind = %v, want Processing", tx.Actions[8].Kind)
	}
	if tx.Actions[9].Verb != "V(s) := 0" {
		t.Errorf("Actions[9].Verb = %q, want %q", tx.Actions[9].Verb, "V(s) := 0")
	}
	if tx.Actions[9].Kind != Processing {
		t.Errorf("Actions[9].Kind = %v, want Processing", tx.Actions[9].Kind)
	}
	if tx.Actions[10].Verb != "V(a) := 0" {
		t.Errorf("Actions[10].Verb = %q, want %q", tx.Actions[10].Verb, "V(a) := 0")
	}
	if tx.Actions[10].Kind != Processing {
		t.Errorf("Actions[10].Kind = %v, want Processing", tx.Actions[10].Kind)
	}
	if tx.Actions[11].Verb != "V(r) := 0" {
		t.Errorf("Actions[11].Verb = %q, want %q", tx.Actions[11].Verb, "V(r) := 0")
	}
	if tx.Actions[11].Kind != Processing {
		t.Errorf("Actions[11].Kind = %v, want Processing", tx.Actions[11].Kind)
	}
}

func TestDataLinkTimerRecovery_t13_sabm_received_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t13_sabm_received_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t13_sabm_received_yes not found")
	}
	if tx.On != "SABM_received" {
		t.Errorf("On = %q, want SABM_received", tx.On)
	}
	if tx.Next != "Connected" {
		t.Errorf("Next = %q, want Connected", tx.Next)
	}
	if tx.Guard != "vs_eq_va" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "vs_eq_va")
	}
	if got := len(tx.Actions); got != 5 {
		t.Fatalf("len(Actions) = %d, want 5", got)
	}
	if tx.Actions[0].Verb != "F := P" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "F := P")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "set_version_2_0" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "set_version_2_0")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "UA" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "UA")
	}
	if tx.Actions[2].Kind != SignalLower {
		t.Errorf("Actions[2].Kind = %v, want SignalLower", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Clear Exception Conditions" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Clear Exception Conditions")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "DL-ERROR Indication (F)" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "DL-ERROR Indication (F)")
	}
	if tx.Actions[4].Kind != SignalUpper {
		t.Errorf("Actions[4].Kind = %v, want SignalUpper", tx.Actions[4].Kind)
	}
}

func TestDataLinkTimerRecovery_t14_sabme_received_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t14_sabme_received_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t14_sabme_received_no not found")
	}
	if tx.On != "SABME_received" {
		t.Errorf("On = %q, want SABME_received", tx.On)
	}
	if tx.Next != "Connected" {
		t.Errorf("Next = %q, want Connected", tx.Next)
	}
	if tx.Guard != "not vs_eq_va" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not vs_eq_va")
	}
	if got := len(tx.Actions); got != 12 {
		t.Fatalf("len(Actions) = %d, want 12", got)
	}
	if tx.Actions[0].Verb != "F := P" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "F := P")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Set Version 2.2" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Set Version 2.2")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "UA" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "UA")
	}
	if tx.Actions[2].Kind != SignalLower {
		t.Errorf("Actions[2].Kind = %v, want SignalLower", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Clear Exception Conditions" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Clear Exception Conditions")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "DL-ERROR Indication (F)" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "DL-ERROR Indication (F)")
	}
	if tx.Actions[4].Kind != SignalUpper {
		t.Errorf("Actions[4].Kind = %v, want SignalUpper", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "DL_CONNECT_indication" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "DL_CONNECT_indication")
	}
	if tx.Actions[6].Kind != SignalUpper {
		t.Errorf("Actions[6].Kind = %v, want SignalUpper", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "Stop T1" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "Stop T1")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
	if tx.Actions[8].Verb != "Stop T3" {
		t.Errorf("Actions[8].Verb = %q, want %q", tx.Actions[8].Verb, "Stop T3")
	}
	if tx.Actions[8].Kind != Processing {
		t.Errorf("Actions[8].Kind = %v, want Processing", tx.Actions[8].Kind)
	}
	if tx.Actions[9].Verb != "V(s) := 0" {
		t.Errorf("Actions[9].Verb = %q, want %q", tx.Actions[9].Verb, "V(s) := 0")
	}
	if tx.Actions[9].Kind != Processing {
		t.Errorf("Actions[9].Kind = %v, want Processing", tx.Actions[9].Kind)
	}
	if tx.Actions[10].Verb != "V(a) := 0" {
		t.Errorf("Actions[10].Verb = %q, want %q", tx.Actions[10].Verb, "V(a) := 0")
	}
	if tx.Actions[10].Kind != Processing {
		t.Errorf("Actions[10].Kind = %v, want Processing", tx.Actions[10].Kind)
	}
	if tx.Actions[11].Verb != "V(r) := 0" {
		t.Errorf("Actions[11].Verb = %q, want %q", tx.Actions[11].Verb, "V(r) := 0")
	}
	if tx.Actions[11].Kind != Processing {
		t.Errorf("Actions[11].Kind = %v, want Processing", tx.Actions[11].Kind)
	}
}

func TestDataLinkTimerRecovery_t14_sabme_received_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t14_sabme_received_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t14_sabme_received_yes not found")
	}
	if tx.On != "SABME_received" {
		t.Errorf("On = %q, want SABME_received", tx.On)
	}
	if tx.Next != "Connected" {
		t.Errorf("Next = %q, want Connected", tx.Next)
	}
	if tx.Guard != "vs_eq_va" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "vs_eq_va")
	}
	if got := len(tx.Actions); got != 5 {
		t.Fatalf("len(Actions) = %d, want 5", got)
	}
	if tx.Actions[0].Verb != "F := P" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "F := P")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Set Version 2.2" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Set Version 2.2")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "UA" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "UA")
	}
	if tx.Actions[2].Kind != SignalLower {
		t.Errorf("Actions[2].Kind = %v, want SignalLower", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Clear Exception Conditions" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Clear Exception Conditions")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "DL-ERROR Indication (F)" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "DL-ERROR Indication (F)")
	}
	if tx.Actions[4].Kind != SignalUpper {
		t.Errorf("Actions[4].Kind = %v, want SignalUpper", tx.Actions[4].Kind)
	}
}

func TestDataLinkTimerRecovery_t15_frmr_received(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t15_frmr_received" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t15_frmr_received not found")
	}
	if tx.On != "FRMR_received" {
		t.Errorf("On = %q, want FRMR_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (K)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (K)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "set_version_2_0" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "set_version_2_0")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Establish_Data_Link" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Establish_Data_Link")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Clear Layer 3 Initiated" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Clear Layer 3 Initiated")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t16_ui_received_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t16_ui_received_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t16_ui_received_yes not found")
	}
	if tx.On != "UI_received" {
		t.Errorf("On = %q, want UI_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "P_eq_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "P_eq_1")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "UI Check" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "UI Check")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t16_ui_received_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t16_ui_received_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t16_ui_received_no not found")
	}
	if tx.On != "UI_received" {
		t.Errorf("On = %q, want UI_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not P_eq_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not P_eq_1")
	}
	if got := len(tx.Actions); got != 1 {
		t.Fatalf("len(Actions) = %d, want 1", got)
	}
	if tx.Actions[0].Verb != "UI Check" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "UI Check")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
}

func TestDataLinkTimerRecovery_t17_disc_received(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t17_disc_received" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t17_disc_received not found")
	}
	if tx.On != "DISC_received" {
		t.Errorf("On = %q, want DISC_received", tx.On)
	}
	if tx.Next != "Disconnected" {
		t.Errorf("Next = %q, want Disconnected", tx.Next)
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "F := P" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "F := P")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "UA" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "UA")
	}
	if tx.Actions[2].Kind != SignalLower {
		t.Errorf("Actions[2].Kind = %v, want SignalLower", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "DL_DISCONNECT_indication" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "DL_DISCONNECT_indication")
	}
	if tx.Actions[3].Kind != SignalUpper {
		t.Errorf("Actions[3].Kind = %v, want SignalUpper", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Stop T1" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Stop T1")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Stop T3" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Stop T3")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t18_rr_received_no_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t18_rr_received_no_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t18_rr_received_no_yes_yes not found")
	}
	if tx.On != "RR_received" {
		t.Errorf("On = %q, want RR_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "V(a) := N(r)")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t18_rr_received_no_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t18_rr_received_no_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t18_rr_received_no_yes_no_no not found")
	}
	if tx.On != "RR_received" {
		t.Errorf("On = %q, want RR_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "N(r) Recovery" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "N(r) Recovery")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t18_rr_received_no_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t18_rr_received_no_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t18_rr_received_no_yes_no_yes not found")
	}
	if tx.On != "RR_received" {
		t.Errorf("On = %q, want RR_received", tx.On)
	}
	if tx.Next != "AwaitingV22Connection" {
		t.Errorf("Next = %q, want AwaitingV22Connection", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "N(r) Recovery" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "N(r) Recovery")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t18_rr_received_no_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t18_rr_received_no_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t18_rr_received_no_no_yes not found")
	}
	if tx.On != "RR_received" {
		t.Errorf("On = %q, want RR_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and not command_and_P_eq_1 and va_le_nr_le_vs" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and not command_and_P_eq_1 and va_le_nr_le_vs")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "V(a) := N(r)")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t18_rr_received_no_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t18_rr_received_no_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t18_rr_received_no_no_no not found")
	}
	if tx.On != "RR_received" {
		t.Errorf("On = %q, want RR_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and not command_and_P_eq_1 and not va_le_nr_le_vs" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and not command_and_P_eq_1 and not va_le_nr_le_vs")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "N(r) Recovery" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "N(r) Recovery")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t18_rr_received_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t18_rr_received_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t18_rr_received_yes_no_no not found")
	}
	if tx.On != "RR_received" {
		t.Errorf("On = %q, want RR_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and not va_le_nr_le_vs and not version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and not va_le_nr_le_vs and not version_2_2")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) Recovery" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) Recovery")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t18_rr_received_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t18_rr_received_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t18_rr_received_yes_no_yes not found")
	}
	if tx.On != "RR_received" {
		t.Errorf("On = %q, want RR_received", tx.On)
	}
	if tx.Next != "AwaitingV22Connection" {
		t.Errorf("Next = %q, want AwaitingV22Connection", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and not va_le_nr_le_vs and version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and not va_le_nr_le_vs and version_2_2")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) Recovery" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) Recovery")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t18_rr_received_yes_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t18_rr_received_yes_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t18_rr_received_yes_yes_no not found")
	}
	if tx.On != "RR_received" {
		t.Errorf("On = %q, want RR_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and va_le_nr_le_vs and not vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and va_le_nr_le_vs and not vs_eq_nr")
	}
	if got := len(tx.Actions); got != 8 {
		t.Fatalf("len(Actions) = %d, want 8", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "V(a) := N(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Invoke Retransmission" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Invoke Retransmission")
	}
	if tx.Actions[4].Kind != Subroutine {
		t.Errorf("Actions[4].Kind = %v, want Subroutine", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Stop T3" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Stop T3")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "Start T1" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "Start T1")
	}
	if tx.Actions[6].Kind != Processing {
		t.Errorf("Actions[6].Kind = %v, want Processing", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "set_acknowledge_pending" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "set_acknowledge_pending")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
}

func TestDataLinkTimerRecovery_t18_rr_received_yes_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t18_rr_received_yes_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t18_rr_received_yes_yes_yes not found")
	}
	if tx.On != "RR_received" {
		t.Errorf("On = %q, want RR_received", tx.On)
	}
	if tx.Next != "Connected" {
		t.Errorf("Next = %q, want Connected", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and va_le_nr_le_vs and vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and va_le_nr_le_vs and vs_eq_nr")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "V(a) := N(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Start T3" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Start T3")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "RC := 0" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "RC := 0")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t19_rnr_received_no_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t19_rnr_received_no_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t19_rnr_received_no_yes_yes not found")
	}
	if tx.On != "RNR_received" {
		t.Errorf("On = %q, want RNR_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "set_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "set_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "V(a) := N(r)")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t19_rnr_received_no_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t19_rnr_received_no_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t19_rnr_received_no_yes_no_no not found")
	}
	if tx.On != "RNR_received" {
		t.Errorf("On = %q, want RNR_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "set_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "set_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "N(r) Recovery" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "N(r) Recovery")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t19_rnr_received_no_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t19_rnr_received_no_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t19_rnr_received_no_yes_no_yes not found")
	}
	if tx.On != "RNR_received" {
		t.Errorf("On = %q, want RNR_received", tx.On)
	}
	if tx.Next != "AwaitingV22Connection" {
		t.Errorf("Next = %q, want AwaitingV22Connection", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "set_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "set_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "N(r) Recovery" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "N(r) Recovery")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t19_rnr_received_no_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t19_rnr_received_no_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t19_rnr_received_no_no_yes not found")
	}
	if tx.On != "RNR_received" {
		t.Errorf("On = %q, want RNR_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and not command_and_P_eq_1 and va_le_nr_le_vs" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and not command_and_P_eq_1 and va_le_nr_le_vs")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "set_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "set_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "V(a) := N(r)")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t19_rnr_received_no_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t19_rnr_received_no_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t19_rnr_received_no_no_no not found")
	}
	if tx.On != "RNR_received" {
		t.Errorf("On = %q, want RNR_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and not command_and_P_eq_1 and not va_le_nr_le_vs" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and not command_and_P_eq_1 and not va_le_nr_le_vs")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "set_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "set_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "N(r) Recovery" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "N(r) Recovery")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t19_rnr_received_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t19_rnr_received_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t19_rnr_received_yes_no_no not found")
	}
	if tx.On != "RNR_received" {
		t.Errorf("On = %q, want RNR_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and not va_le_nr_le_vs and not version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and not va_le_nr_le_vs and not version_2_2")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "set_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "set_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) Recovery" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) Recovery")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t19_rnr_received_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t19_rnr_received_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t19_rnr_received_yes_no_yes not found")
	}
	if tx.On != "RNR_received" {
		t.Errorf("On = %q, want RNR_received", tx.On)
	}
	if tx.Next != "AwaitingV22Connection" {
		t.Errorf("Next = %q, want AwaitingV22Connection", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and not va_le_nr_le_vs and version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and not va_le_nr_le_vs and version_2_2")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "set_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "set_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) Recovery" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) Recovery")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t19_rnr_received_yes_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t19_rnr_received_yes_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t19_rnr_received_yes_yes_no not found")
	}
	if tx.On != "RNR_received" {
		t.Errorf("On = %q, want RNR_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and va_le_nr_le_vs and not vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and va_le_nr_le_vs and not vs_eq_nr")
	}
	if got := len(tx.Actions); got != 8 {
		t.Fatalf("len(Actions) = %d, want 8", got)
	}
	if tx.Actions[0].Verb != "set_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "set_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "V(a) := N(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Invoke Retransmission" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Invoke Retransmission")
	}
	if tx.Actions[4].Kind != Subroutine {
		t.Errorf("Actions[4].Kind = %v, want Subroutine", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Stop T3" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Stop T3")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "Start T1" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "Start T1")
	}
	if tx.Actions[6].Kind != Processing {
		t.Errorf("Actions[6].Kind = %v, want Processing", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "set_acknowledge_pending" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "set_acknowledge_pending")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
}

func TestDataLinkTimerRecovery_t19_rnr_received_yes_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t19_rnr_received_yes_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t19_rnr_received_yes_yes_yes not found")
	}
	if tx.On != "RNR_received" {
		t.Errorf("On = %q, want RNR_received", tx.On)
	}
	if tx.Next != "Connected" {
		t.Errorf("Next = %q, want Connected", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and va_le_nr_le_vs and vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and va_le_nr_le_vs and vs_eq_nr")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "set_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "set_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "V(a) := N(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Start T3" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Start T3")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "RC := 0" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "RC := 0")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t20_lm_seize_confirm_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t20_lm_seize_confirm_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t20_lm_seize_confirm_yes not found")
	}
	if tx.On != "LM_SEIZE_confirm" {
		t.Errorf("On = %q, want LM_SEIZE_confirm", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "ack_pending" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "ack_pending")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry Response (F = 0)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry Response (F = 0)")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "LM_release_request" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "LM_release_request")
	}
	if tx.Actions[2].Kind != SignalLower {
		t.Errorf("Actions[2].Kind = %v, want SignalLower", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t20_lm_seize_confirm_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t20_lm_seize_confirm_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t20_lm_seize_confirm_no not found")
	}
	if tx.On != "LM_SEIZE_confirm" {
		t.Errorf("On = %q, want LM_SEIZE_confirm", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not ack_pending" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not ack_pending")
	}
	if got := len(tx.Actions); got != 1 {
		t.Fatalf("len(Actions) = %d, want 1", got)
	}
	if tx.Actions[0].Verb != "LM_release_request" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "LM_release_request")
	}
	if tx.Actions[0].Kind != SignalLower {
		t.Errorf("Actions[0].Kind = %v, want SignalLower", tx.Actions[0].Kind)
	}
}

func TestDataLinkTimerRecovery_t21_t1_expiry_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t21_t1_expiry_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t21_t1_expiry_no not found")
	}
	if tx.On != "T1_expiry" {
		t.Errorf("On = %q, want T1_expiry", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not RC_eq_N2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not RC_eq_N2")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "Transmit Enquiry" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Transmit Enquiry")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "RC := RC + 1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "RC := RC + 1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t21_t1_expiry_yes_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t21_t1_expiry_yes_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t21_t1_expiry_yes_yes_yes not found")
	}
	if tx.On != "T1_expiry" {
		t.Errorf("On = %q, want T1_expiry", tx.On)
	}
	if tx.Next != "Disconnected" {
		t.Errorf("Next = %q, want Disconnected", tx.Next)
	}
	if tx.Guard != "RC_eq_N2 and vs_eq_va and peer_receiver_busy" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "RC_eq_N2 and vs_eq_va and peer_receiver_busy")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (U)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (U)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "DL_DISCONNECT_indication" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "DL_DISCONNECT_indication")
	}
	if tx.Actions[1].Kind != SignalUpper {
		t.Errorf("Actions[1].Kind = %v, want SignalUpper", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "DM Response (F = 0)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "DM Response (F = 0)")
	}
	if tx.Actions[3].Kind != SignalLower {
		t.Errorf("Actions[3].Kind = %v, want SignalLower", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t21_t1_expiry_yes_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t21_t1_expiry_yes_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t21_t1_expiry_yes_yes_no not found")
	}
	if tx.On != "T1_expiry" {
		t.Errorf("On = %q, want T1_expiry", tx.On)
	}
	if tx.Next != "Disconnected" {
		t.Errorf("Next = %q, want Disconnected", tx.Next)
	}
	if tx.Guard != "RC_eq_N2 and vs_eq_va and not peer_receiver_busy" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "RC_eq_N2 and vs_eq_va and not peer_receiver_busy")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (T)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (T)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "DL_DISCONNECT_indication" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "DL_DISCONNECT_indication")
	}
	if tx.Actions[1].Kind != SignalUpper {
		t.Errorf("Actions[1].Kind = %v, want SignalUpper", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "DM Response (F = 0)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "DM Response (F = 0)")
	}
	if tx.Actions[3].Kind != SignalLower {
		t.Errorf("Actions[3].Kind = %v, want SignalLower", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t21_t1_expiry_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t21_t1_expiry_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t21_t1_expiry_yes_no not found")
	}
	if tx.On != "T1_expiry" {
		t.Errorf("On = %q, want T1_expiry", tx.On)
	}
	if tx.Next != "Disconnected" {
		t.Errorf("Next = %q, want Disconnected", tx.Next)
	}
	if tx.Guard != "RC_eq_N2 and not vs_eq_va" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "RC_eq_N2 and not vs_eq_va")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (I)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (I)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "DL_DISCONNECT_indication" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "DL_DISCONNECT_indication")
	}
	if tx.Actions[1].Kind != SignalUpper {
		t.Errorf("Actions[1].Kind = %v, want SignalUpper", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "discard_I_frame_queue" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "discard_I_frame_queue")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "DM Response (F = 0)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "DM Response (F = 0)")
	}
	if tx.Actions[3].Kind != SignalLower {
		t.Errorf("Actions[3].Kind = %v, want SignalLower", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_no not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "Connected" {
		t.Errorf("Next = %q, want Connected", tx.Next)
	}
	if tx.Guard != "not command" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not command")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (O)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (O)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Discard I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Discard I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_no not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "command and not info_field_length_le_N1_and_content_is_octet_aligned" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and not info_field_length_le_N1_and_content_is_octet_aligned")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "DL-ERROR Indication (O)" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "DL-ERROR Indication (O)")
	}
	if tx.Actions[0].Kind != SignalUpper {
		t.Errorf("Actions[0].Kind = %v, want SignalUpper", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Establish_Data_Link" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Establish_Data_Link")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Clear Layer 3 Initiated" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Clear Layer 3 Initiated")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_no not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and not va_le_nr_le_vs" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and not va_le_nr_le_vs")
	}
	if got := len(tx.Actions); got != 1 {
		t.Fatalf("len(Actions) = %d, want 1", got)
	}
	if tx.Actions[0].Verb != "N(r) Recovery" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "N(r) Recovery")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_yes_yes not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and own_receiver_busy and P_eq_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and own_receiver_busy and P_eq_1")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Discard Contents of I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Discard Contents of I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "F := 1" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "F := 1")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) := V(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) := V(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "RNR" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "RNR")
	}
	if tx.Actions[4].Kind != SignalLower {
		t.Errorf("Actions[4].Kind = %v, want SignalLower", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_yes_no not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and own_receiver_busy and not P_eq_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and own_receiver_busy and not P_eq_1")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Discard Contents of I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Discard Contents of I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_yes_yes not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and ns_eq_vr and P_eq_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and ns_eq_vr and P_eq_1")
	}
	if got := len(tx.Actions); got != 12 {
		t.Fatalf("len(Actions) = %d, want 12", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "V(r) := V(r) + 1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "V(r) := V(r) + 1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Clear Reject Exception" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Clear Reject Exception")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Decrement Sreject Exception if > 0" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Decrement Sreject Exception if > 0")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "DL_DATA_indication" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "DL_DATA_indication")
	}
	if tx.Actions[4].Kind != SignalUpper {
		t.Errorf("Actions[4].Kind = %v, want SignalUpper", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Retrieve Stored V(r) I Frame" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Retrieve Stored V(r) I Frame")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "DL_DATA_indication" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "DL_DATA_indication")
	}
	if tx.Actions[6].Kind != SignalUpper {
		t.Errorf("Actions[6].Kind = %v, want SignalUpper", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "V(r) := V(r) - 1" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "V(r) := V(r) - 1")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
	if tx.Actions[8].Verb != "F := 1" {
		t.Errorf("Actions[8].Verb = %q, want %q", tx.Actions[8].Verb, "F := 1")
	}
	if tx.Actions[8].Kind != Processing {
		t.Errorf("Actions[8].Kind = %v, want Processing", tx.Actions[8].Kind)
	}
	if tx.Actions[9].Verb != "N(r) := V(r)" {
		t.Errorf("Actions[9].Verb = %q, want %q", tx.Actions[9].Verb, "N(r) := V(r)")
	}
	if tx.Actions[9].Kind != Processing {
		t.Errorf("Actions[9].Kind = %v, want Processing", tx.Actions[9].Kind)
	}
	if tx.Actions[10].Verb != "RR" {
		t.Errorf("Actions[10].Verb = %q, want %q", tx.Actions[10].Verb, "RR")
	}
	if tx.Actions[10].Kind != SignalLower {
		t.Errorf("Actions[10].Kind = %v, want SignalLower", tx.Actions[10].Kind)
	}
	if tx.Actions[11].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[11].Verb = %q, want %q", tx.Actions[11].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[11].Kind != Processing {
		t.Errorf("Actions[11].Kind = %v, want Processing", tx.Actions[11].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_yes_no_no not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and ns_eq_vr and not P_eq_1 and not ack_pending" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and ns_eq_vr and not P_eq_1 and not ack_pending")
	}
	if got := len(tx.Actions); got != 10 {
		t.Fatalf("len(Actions) = %d, want 10", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "V(r) := V(r) + 1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "V(r) := V(r) + 1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Clear Reject Exception" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Clear Reject Exception")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Decrement Sreject Exception if > 0" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Decrement Sreject Exception if > 0")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "DL_DATA_indication" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "DL_DATA_indication")
	}
	if tx.Actions[4].Kind != SignalUpper {
		t.Errorf("Actions[4].Kind = %v, want SignalUpper", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Retrieve Stored V(r) I Frame" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Retrieve Stored V(r) I Frame")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "DL_DATA_indication" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "DL_DATA_indication")
	}
	if tx.Actions[6].Kind != SignalUpper {
		t.Errorf("Actions[6].Kind = %v, want SignalUpper", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "V(r) := V(r) - 1" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "V(r) := V(r) - 1")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
	if tx.Actions[8].Verb != "set_acknowledge_pending" {
		t.Errorf("Actions[8].Verb = %q, want %q", tx.Actions[8].Verb, "set_acknowledge_pending")
	}
	if tx.Actions[8].Kind != Processing {
		t.Errorf("Actions[8].Kind = %v, want Processing", tx.Actions[8].Kind)
	}
	if tx.Actions[9].Verb != "LM_seize_request" {
		t.Errorf("Actions[9].Verb = %q, want %q", tx.Actions[9].Verb, "LM_seize_request")
	}
	if tx.Actions[9].Kind != SignalLower {
		t.Errorf("Actions[9].Kind = %v, want SignalLower", tx.Actions[9].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_yes_no_yes not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and ns_eq_vr and not P_eq_1 and ack_pending" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and ns_eq_vr and not P_eq_1 and ack_pending")
	}
	if got := len(tx.Actions); got != 8 {
		t.Fatalf("len(Actions) = %d, want 8", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "V(r) := V(r) + 1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "V(r) := V(r) + 1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Clear Reject Exception" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Clear Reject Exception")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Decrement Sreject Exception if > 0" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Decrement Sreject Exception if > 0")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "DL_DATA_indication" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "DL_DATA_indication")
	}
	if tx.Actions[4].Kind != SignalUpper {
		t.Errorf("Actions[4].Kind = %v, want SignalUpper", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Retrieve Stored V(r) I Frame" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Retrieve Stored V(r) I Frame")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "DL_DATA_indication" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "DL_DATA_indication")
	}
	if tx.Actions[6].Kind != SignalUpper {
		t.Errorf("Actions[6].Kind = %v, want SignalUpper", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "V(r) := V(r) - 1" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "V(r) := V(r) - 1")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_no_yes_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_no_yes_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_no_yes_yes_yes not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and reject_exception and P_eq_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and reject_exception and P_eq_1")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Discard Contents of I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Discard Contents of I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "F := 1" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "F := 1")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) := V(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) := V(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "RR" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "RR")
	}
	if tx.Actions[4].Kind != SignalLower {
		t.Errorf("Actions[4].Kind = %v, want SignalLower", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_no_yes_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_no_yes_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_no_yes_yes_no not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and reject_exception and not P_eq_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and reject_exception and not P_eq_1")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Discard Contents of I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Discard Contents of I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_no_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_no_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_no_yes_no_no not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and not reject_exception and not SREJ_enabled" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and not reject_exception and not SREJ_enabled")
	}
	if got := len(tx.Actions); got != 7 {
		t.Fatalf("len(Actions) = %d, want 7", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Discard Contents of I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Discard Contents of I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Set Reject Exception" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Set Reject Exception")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "F := P" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "F := P")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "N(r) := V(r)" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "N(r) := V(r)")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "REJ" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "REJ")
	}
	if tx.Actions[5].Kind != SignalLower {
		t.Errorf("Actions[5].Kind = %v, want SignalLower", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "set_acknowledge_pending" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "set_acknowledge_pending")
	}
	if tx.Actions[6].Kind != Processing {
		t.Errorf("Actions[6].Kind = %v, want Processing", tx.Actions[6].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_no_yes_no_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_no_yes_no_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_no_yes_no_yes_no_yes not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and not reject_exception and SREJ_enabled and not sreject_exception_gt_0 and ns_gt_vr_plus_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and not reject_exception and SREJ_enabled and not sreject_exception_gt_0 and ns_gt_vr_plus_1")
	}
	if got := len(tx.Actions); got != 8 {
		t.Fatalf("len(Actions) = %d, want 8", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Save Contents of I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Save Contents of I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Discard Contents of I Frame" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Discard Contents of I Frame")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Set Reject Exception" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Set Reject Exception")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "F := P" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "F := P")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "N(r) := V(r)" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "N(r) := V(r)")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "REJ" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "REJ")
	}
	if tx.Actions[6].Kind != SignalLower {
		t.Errorf("Actions[6].Kind = %v, want SignalLower", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "set_acknowledge_pending" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "set_acknowledge_pending")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_no_yes_no_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_no_yes_no_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_no_yes_no_yes_no_no not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and not reject_exception and SREJ_enabled and not sreject_exception_gt_0 and not ns_gt_vr_plus_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and not reject_exception and SREJ_enabled and not sreject_exception_gt_0 and not ns_gt_vr_plus_1")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Save Contents of I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Save Contents of I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "N(r) := V(r)" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "N(r) := V(r)")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "F := 1" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "F := 1")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Sreject := Sreject + 1" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Sreject := Sreject + 1")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "SREJ" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "SREJ")
	}
	if tx.Actions[5].Kind != SignalLower {
		t.Errorf("Actions[5].Kind = %v, want SignalLower", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_no_yes_no_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_no_yes_no_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_no_yes_no_yes_yes not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and not reject_exception and SREJ_enabled and sreject_exception_gt_0" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and vr_lt_ns_lt_vr_plus_k and not reject_exception and SREJ_enabled and sreject_exception_gt_0")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Save Contents of I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Save Contents of I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "N(r) := V(r)" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "N(r) := V(r)")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "F := 0" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "F := 0")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Sreject := Sreject + 1" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Sreject := Sreject + 1")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "SREJ" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "SREJ")
	}
	if tx.Actions[5].Kind != SignalLower {
		t.Errorf("Actions[5].Kind = %v, want SignalLower", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_no_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_no_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_no_no_yes not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and not vr_lt_ns_lt_vr_plus_k and P_eq_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and not vr_lt_ns_lt_vr_plus_k and P_eq_1")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Discard Contents of I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Discard Contents of I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "F := 1" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "F := 1")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) := V(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) := V(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "RR" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "RR")
	}
	if tx.Actions[4].Kind != SignalLower {
		t.Errorf("Actions[4].Kind = %v, want SignalLower", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t22_i_received_yes_yes_yes_no_no_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t22_i_received_yes_yes_yes_no_no_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t22_i_received_yes_yes_yes_no_no_no_no not found")
	}
	if tx.On != "I_received" {
		t.Errorf("On = %q, want I_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and not vr_lt_ns_lt_vr_plus_k and not P_eq_1" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "command and info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receiver_busy and not ns_eq_vr and not vr_lt_ns_lt_vr_plus_k and not P_eq_1")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[0].Kind != Subroutine {
		t.Errorf("Actions[0].Kind = %v, want Subroutine", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Discard Contents of I Frame" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Discard Contents of I Frame")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_no_yes_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_no_yes_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_no_yes_yes_yes not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs and vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs and vs_eq_nr")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "V(a) := N(r)")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_no_yes_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_no_yes_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_no_yes_yes_no not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs and not vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs and not vs_eq_nr")
	}
	if got := len(tx.Actions); got != 7 {
		t.Fatalf("len(Actions) = %d, want 7", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "V(a) := N(r)")
	}
	if tx.Actions[2].Kind != Processing {
		t.Errorf("Actions[2].Kind = %v, want Processing", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Invoke Retransmission" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Invoke Retransmission")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Stop T3" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Stop T3")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Start T1" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Start T1")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[6].Kind != Processing {
		t.Errorf("Actions[6].Kind = %v, want Processing", tx.Actions[6].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_no_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_no_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_no_yes_no_yes not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "AwaitingV22Connection" {
		t.Errorf("Next = %q, want AwaitingV22Connection", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "N(r) Recovery" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "N(r) Recovery")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_no_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_no_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_no_yes_no_no not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Enquiry_Response_F_1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Enquiry_Response_F_1")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "N(r) Recovery" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "N(r) Recovery")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_no_no_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_no_no_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_no_no_yes_yes not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and not command_and_P_eq_1 and va_le_nr_le_vs and vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and not command_and_P_eq_1 and va_le_nr_le_vs and vs_eq_nr")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "V(a) := N(r)")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_no_no_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_no_no_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_no_no_yes_no not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and not command_and_P_eq_1 and va_le_nr_le_vs and not vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and not command_and_P_eq_1 and va_le_nr_le_vs and not vs_eq_nr")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "V(a) := N(r)")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Invoke Retransmission" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Invoke Retransmission")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Stop T3" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Stop T3")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Start T1" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Start T1")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_no_no_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_no_no_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_no_no_no_yes not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "AwaitingV22Connection" {
		t.Errorf("Next = %q, want AwaitingV22Connection", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and not command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and not command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "N(r) Recovery" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "N(r) Recovery")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_no_no_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_no_no_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_no_no_no_no not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "not response_and_F_eq_1 and not command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response_and_F_eq_1 and not command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "N(r) Recovery" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "N(r) Recovery")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_yes_no_yes not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "AwaitingV22Connection" {
		t.Errorf("Next = %q, want AwaitingV22Connection", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and not va_le_nr_le_vs and version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and not va_le_nr_le_vs and version_2_2")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) Recovery" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) Recovery")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_yes_no_no not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and not va_le_nr_le_vs and not version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and not va_le_nr_le_vs and not version_2_2")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) Recovery" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) Recovery")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_yes_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_yes_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_yes_yes_yes not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "Connected" {
		t.Errorf("Next = %q, want Connected", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and va_le_nr_le_vs and vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and va_le_nr_le_vs and vs_eq_nr")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "V(a) := N(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Start T3" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Start T3")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "RC := 0" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "RC := 0")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t23_rej_received_yes_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t23_rej_received_yes_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t23_rej_received_yes_yes_no not found")
	}
	if tx.On != "REJ_received" {
		t.Errorf("On = %q, want REJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "response_and_F_eq_1 and va_le_nr_le_vs and not vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response_and_F_eq_1 and va_le_nr_le_vs and not vs_eq_nr")
	}
	if got := len(tx.Actions); got != 8 {
		t.Fatalf("len(Actions) = %d, want 8", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "V(a) := N(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Invoke Retransmission" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Invoke Retransmission")
	}
	if tx.Actions[4].Kind != Subroutine {
		t.Errorf("Actions[4].Kind = %v, want Subroutine", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Stop T3" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Stop T3")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "Start T1" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "Start T1")
	}
	if tx.Actions[6].Kind != Processing {
		t.Errorf("Actions[6].Kind = %v, want Processing", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_no_yes_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_no_yes_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_no_yes_yes_yes not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response and va_le_nr_le_vs and P_eq_1 and vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response and va_le_nr_le_vs and P_eq_1 and vs_eq_nr")
	}
	if got := len(tx.Actions); got != 3 {
		t.Fatalf("len(Actions) = %d, want 3", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "V(a) := N(r)")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_no_yes_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_no_yes_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_no_yes_yes_no not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response and va_le_nr_le_vs and P_eq_1 and not vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response and va_le_nr_le_vs and P_eq_1 and not vs_eq_nr")
	}
	if got := len(tx.Actions); got != 8 {
		t.Fatalf("len(Actions) = %d, want 8", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "V(a) := N(r)")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Push Old I Frame N(r) on Queue" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Push Old I Frame N(r) on Queue")
	}
	if tx.Actions[3].Kind != InternalOut {
		t.Errorf("Actions[3].Kind = %v, want InternalOut", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "LM_data_request" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "LM_data_request")
	}
	if tx.Actions[4].Kind != SignalLower {
		t.Errorf("Actions[4].Kind = %v, want SignalLower", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Stop T3" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Stop T3")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "Start T1" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "Start T1")
	}
	if tx.Actions[6].Kind != Processing {
		t.Errorf("Actions[6].Kind = %v, want Processing", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_no_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_no_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_no_yes_no_yes not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response and va_le_nr_le_vs and not P_eq_1 and vs_eq_va" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response and va_le_nr_le_vs and not P_eq_1 and vs_eq_va")
	}
	if got := len(tx.Actions); got != 1 {
		t.Fatalf("len(Actions) = %d, want 1", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_no_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_no_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_no_yes_no_no not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "not response and va_le_nr_le_vs and not P_eq_1 and not vs_eq_va" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response and va_le_nr_le_vs and not P_eq_1 and not vs_eq_va")
	}
	if got := len(tx.Actions); got != 6 {
		t.Fatalf("len(Actions) = %d, want 6", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Push Old I Frame N(r) on Queue" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Push Old I Frame N(r) on Queue")
	}
	if tx.Actions[1].Kind != InternalOut {
		t.Errorf("Actions[1].Kind = %v, want InternalOut", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "LM_data_request" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "LM_data_request")
	}
	if tx.Actions[2].Kind != SignalLower {
		t.Errorf("Actions[2].Kind = %v, want SignalLower", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Stop T3" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Stop T3")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Start T1" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Start T1")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_no_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_no_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_no_no_no not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "not response and not va_le_nr_le_vs and not version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response and not va_le_nr_le_vs and not version_2_2")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "N(r) Recovery" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "N(r) Recovery")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_no_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_no_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_no_no_yes not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "AwaitingV22Connection" {
		t.Errorf("Next = %q, want AwaitingV22Connection", tx.Next)
	}
	if tx.Guard != "not response and not va_le_nr_le_vs and version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "not response and not va_le_nr_le_vs and version_2_2")
	}
	if got := len(tx.Actions); got != 2 {
		t.Fatalf("len(Actions) = %d, want 2", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "N(r) Recovery" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "N(r) Recovery")
	}
	if tx.Actions[1].Kind != Subroutine {
		t.Errorf("Actions[1].Kind = %v, want Subroutine", tx.Actions[1].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_yes_no_no not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "AwaitingConnection" {
		t.Errorf("Next = %q, want AwaitingConnection", tx.Next)
	}
	if tx.Guard != "response and not va_le_nr_le_vs and not version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response and not va_le_nr_le_vs and not version_2_2")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) Recovery" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) Recovery")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_yes_no_yes not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "AwaitingV22Connection" {
		t.Errorf("Next = %q, want AwaitingV22Connection", tx.Next)
	}
	if tx.Guard != "response and not va_le_nr_le_vs and version_2_2" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response and not va_le_nr_le_vs and version_2_2")
	}
	if got := len(tx.Actions); got != 4 {
		t.Fatalf("len(Actions) = %d, want 4", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "N(r) Recovery" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "N(r) Recovery")
	}
	if tx.Actions[3].Kind != Subroutine {
		t.Errorf("Actions[3].Kind = %v, want Subroutine", tx.Actions[3].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_yes_yes_yes_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_yes_yes_yes_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_yes_yes_yes_yes not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "Connected" {
		t.Errorf("Next = %q, want Connected", tx.Next)
	}
	if tx.Guard != "response and va_le_nr_le_vs and F_eq_1 and vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response and va_le_nr_le_vs and F_eq_1 and vs_eq_nr")
	}
	if got := len(tx.Actions); got != 7 {
		t.Fatalf("len(Actions) = %d, want 7", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "V(a) := N(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[4].Kind != Subroutine {
		t.Errorf("Actions[4].Kind = %v, want Subroutine", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Start T3" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Start T3")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "RC := 0" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "RC := 0")
	}
	if tx.Actions[6].Kind != Processing {
		t.Errorf("Actions[6].Kind = %v, want Processing", tx.Actions[6].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_yes_yes_yes_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_yes_yes_yes_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_yes_yes_yes_no not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "response and va_le_nr_le_vs and F_eq_1 and not vs_eq_nr" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response and va_le_nr_le_vs and F_eq_1 and not vs_eq_nr")
	}
	if got := len(tx.Actions); got != 10 {
		t.Fatalf("len(Actions) = %d, want 10", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "V(a) := N(r)" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "V(a) := N(r)")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "Check_I_Frame_Acknowledged" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "Check_I_Frame_Acknowledged")
	}
	if tx.Actions[4].Kind != Subroutine {
		t.Errorf("Actions[4].Kind = %v, want Subroutine", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Push Old I Frame N(r) on Queue" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Push Old I Frame N(r) on Queue")
	}
	if tx.Actions[5].Kind != InternalOut {
		t.Errorf("Actions[5].Kind = %v, want InternalOut", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "LM_data_request" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "LM_data_request")
	}
	if tx.Actions[6].Kind != SignalLower {
		t.Errorf("Actions[6].Kind = %v, want SignalLower", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "Stop T3" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "Stop T3")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
	if tx.Actions[8].Verb != "Start T1" {
		t.Errorf("Actions[8].Verb = %q, want %q", tx.Actions[8].Verb, "Start T1")
	}
	if tx.Actions[8].Kind != Processing {
		t.Errorf("Actions[8].Kind = %v, want Processing", tx.Actions[8].Kind)
	}
	if tx.Actions[9].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[9].Verb = %q, want %q", tx.Actions[9].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[9].Kind != Processing {
		t.Errorf("Actions[9].Kind = %v, want Processing", tx.Actions[9].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_yes_yes_no_yes(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_yes_yes_no_yes" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_yes_yes_no_yes not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "Connected" {
		t.Errorf("Next = %q, want Connected", tx.Next)
	}
	if tx.Guard != "response and va_le_nr_le_vs and not F_eq_1 and vs_eq_va" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response and va_le_nr_le_vs and not F_eq_1 and vs_eq_va")
	}
	if got := len(tx.Actions); got != 5 {
		t.Fatalf("len(Actions) = %d, want 5", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Start T3" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Start T3")
	}
	if tx.Actions[3].Kind != Processing {
		t.Errorf("Actions[3].Kind = %v, want Processing", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "RC := 0" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "RC := 0")
	}
	if tx.Actions[4].Kind != Processing {
		t.Errorf("Actions[4].Kind = %v, want Processing", tx.Actions[4].Kind)
	}
}

func TestDataLinkTimerRecovery_t24_srej_received_yes_yes_no_no(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t24_srej_received_yes_yes_no_no" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t24_srej_received_yes_yes_no_no not found")
	}
	if tx.On != "SREJ_received" {
		t.Errorf("On = %q, want SREJ_received", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if tx.Guard != "response and va_le_nr_le_vs and not F_eq_1 and not vs_eq_va" {
		t.Errorf("Guard = %q, want %q", tx.Guard, "response and va_le_nr_le_vs and not F_eq_1 and not vs_eq_va")
	}
	if got := len(tx.Actions); got != 8 {
		t.Fatalf("len(Actions) = %d, want 8", got)
	}
	if tx.Actions[0].Verb != "clear_peer_receiver_busy" {
		t.Errorf("Actions[0].Verb = %q, want %q", tx.Actions[0].Verb, "clear_peer_receiver_busy")
	}
	if tx.Actions[0].Kind != Processing {
		t.Errorf("Actions[0].Kind = %v, want Processing", tx.Actions[0].Kind)
	}
	if tx.Actions[1].Verb != "Stop T1" {
		t.Errorf("Actions[1].Verb = %q, want %q", tx.Actions[1].Verb, "Stop T1")
	}
	if tx.Actions[1].Kind != Processing {
		t.Errorf("Actions[1].Kind = %v, want Processing", tx.Actions[1].Kind)
	}
	if tx.Actions[2].Verb != "Select_T1_Value" {
		t.Errorf("Actions[2].Verb = %q, want %q", tx.Actions[2].Verb, "Select_T1_Value")
	}
	if tx.Actions[2].Kind != Subroutine {
		t.Errorf("Actions[2].Kind = %v, want Subroutine", tx.Actions[2].Kind)
	}
	if tx.Actions[3].Verb != "Push Old I Frame N(r) on Queue" {
		t.Errorf("Actions[3].Verb = %q, want %q", tx.Actions[3].Verb, "Push Old I Frame N(r) on Queue")
	}
	if tx.Actions[3].Kind != InternalOut {
		t.Errorf("Actions[3].Kind = %v, want InternalOut", tx.Actions[3].Kind)
	}
	if tx.Actions[4].Verb != "LM_data_request" {
		t.Errorf("Actions[4].Verb = %q, want %q", tx.Actions[4].Verb, "LM_data_request")
	}
	if tx.Actions[4].Kind != SignalLower {
		t.Errorf("Actions[4].Kind = %v, want SignalLower", tx.Actions[4].Kind)
	}
	if tx.Actions[5].Verb != "Stop T3" {
		t.Errorf("Actions[5].Verb = %q, want %q", tx.Actions[5].Verb, "Stop T3")
	}
	if tx.Actions[5].Kind != Processing {
		t.Errorf("Actions[5].Kind = %v, want Processing", tx.Actions[5].Kind)
	}
	if tx.Actions[6].Verb != "Start T1" {
		t.Errorf("Actions[6].Verb = %q, want %q", tx.Actions[6].Verb, "Start T1")
	}
	if tx.Actions[6].Kind != Processing {
		t.Errorf("Actions[6].Kind = %v, want Processing", tx.Actions[6].Kind)
	}
	if tx.Actions[7].Verb != "Clear Acknowledge Pending" {
		t.Errorf("Actions[7].Verb = %q, want %q", tx.Actions[7].Verb, "Clear Acknowledge Pending")
	}
	if tx.Actions[7].Kind != Processing {
		t.Errorf("Actions[7].Kind = %v, want Processing", tx.Actions[7].Kind)
	}
}

func TestDataLinkTimerRecovery_t25_all_other_primitives__from_lower_layer(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t25_all_other_primitives__from_lower_layer" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t25_all_other_primitives__from_lower_layer not found")
	}
	if tx.On != "all_other_primitives__from_lower_layer" {
		t.Errorf("On = %q, want all_other_primitives__from_lower_layer", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if got := len(tx.Actions); got != 0 {
		t.Fatalf("len(Actions) = %d, want 0", got)
	}
}

func TestDataLinkTimerRecovery_t26_all_other_primitives__from_upper_layer(t *testing.T) {
	var tx *TransitionSpec
	for i := range DataLinkTimerRecovery.Transitions {
		if DataLinkTimerRecovery.Transitions[i].ID == "t26_all_other_primitives__from_upper_layer" {
			tx = &DataLinkTimerRecovery.Transitions[i]
			break
		}
	}
	if tx == nil {
		t.Fatalf("transition t26_all_other_primitives__from_upper_layer not found")
	}
	if tx.On != "all_other_primitives__from_upper_layer" {
		t.Errorf("On = %q, want all_other_primitives__from_upper_layer", tx.On)
	}
	if tx.Next != "TimerRecovery" {
		t.Errorf("Next = %q, want TimerRecovery", tx.Next)
	}
	if got := len(tx.Actions); got != 0 {
		t.Fatalf("len(Actions) = %d, want 0", got)
	}
}
